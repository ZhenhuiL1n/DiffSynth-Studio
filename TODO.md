1. Global token and ip-adapter to finetune the model to behave like a real transformation
2. Render the example dataset to check if we can 
    2.1 fit a small scale of data?
    2.2 one image fitting experiment
